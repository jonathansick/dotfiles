# Aliases for Homebrew, the Mac package manager

# From oh-my-zsh:
alias brews='brew list -1'
# Good one from Seth Brown: http://cl.ly/image/1A073P3n1O00
alias brewer='brew update && brew upgrade && brew cleanup && brew doctor'
